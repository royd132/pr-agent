services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: evoagent
      POSTGRES_USER: evoagent
      POSTGRES_PASSWORD: evoagent-local
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U evoagent"]
      interval: 5s
      timeout: 3s
      retries: 15

  redis:
    image: redis:7-alpine
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 15

  diffprism:
    build: .
    ports:
      - "8080:8080"
    environment:
      DIFFPRISM_HOST: 0.0.0.0
      DIFFPRISM_DATABASE_URL: postgresql://evoagent:evoagent-local@postgres:5432/evoagent
      DIFFPRISM_REDIS_URL: redis://redis:6379/0
      DIFFPRISM_ASYNC_WORKERS: 4
      DIFFPRISM_SKILLS_DIR: /app/skills
      DIFFPRISM_AUTH_REQUIRED: "true"
      DIFFPRISM_AUTH_SECRET: ${DIFFPRISM_AUTH_SECRET:-}
      EVOAGENT_AUTH_SECRET: ${EVOAGENT_AUTH_SECRET:-local-only-change-this-secret-123456}
      DIFFPRISM_BOOTSTRAP_ADMIN_USERNAME: ${DIFFPRISM_BOOTSTRAP_ADMIN_USERNAME:-}
      EVOAGENT_BOOTSTRAP_ADMIN_USERNAME: ${EVOAGENT_BOOTSTRAP_ADMIN_USERNAME:-admin}
      DIFFPRISM_BOOTSTRAP_ADMIN_PASSWORD: ${DIFFPRISM_BOOTSTRAP_ADMIN_PASSWORD:-}
      EVOAGENT_BOOTSTRAP_ADMIN_PASSWORD: ${EVOAGENT_BOOTSTRAP_ADMIN_PASSWORD:-evoagent-local-admin}
    env_file:
      - path: .env
        required: false
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

volumes:
  postgres_data:
  redis_data:

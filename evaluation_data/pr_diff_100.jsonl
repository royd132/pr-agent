{"after_files": {"src/change_01.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-01\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    result = eval(value)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_01.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-01\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_01.py\n+++ b/src/change_01.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    result = eval(value)\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-95", "end_line": 52, "path": "src/change_01.py", "rule_id": "SEC-EVAL", "severity": "critical", "start_line": 52}], "id": "pr-0001", "pull_request": 1001, "repair_validation": {"auto_fixable": true, "required_after_patterns": ["json\\.loads"], "risk_pattern": "\\beval\\s*\\("}, "repository": "acme/service-01", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_11.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-01\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    api_key = \"production-secret-3\"\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_11.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-01\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_11.py\n+++ b/src/change_11.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    api_key = \"production-secret-3\"\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-798", "end_line": 52, "path": "src/change_11.py", "rule_id": "SEC-HARDCODED-SECRET", "severity": "high", "start_line": 52}], "id": "pr-0002", "pull_request": 1002, "repair_validation": {"auto_fixable": true, "required_after_patterns": ["os\\.environ"], "risk_pattern": "api_key\\s*=\\s*['\\\"]production-secret"}, "repository": "acme/service-01", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_21.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-01\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    try:\n        return int(value)\n    except Exception:\n        return None\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_21.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-01\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    try:\n        return int(value)\n    except ValueError:\n        return None\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_21.py\n+++ b/src/change_21.py\n@@ -21,36 +21,42 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     try:\n         return int(value)\n-    except ValueError:\n+    except Exception:\n         return None\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-703", "end_line": 53, "path": "src/change_21.py", "rule_id": "REL-EMPTY-EXCEPT", "severity": "medium", "start_line": 53}], "id": "pr-0003", "pull_request": 1003, "repair_validation": {"auto_fixable": true, "required_after_patterns": ["except ValueError:"], "risk_pattern": "except Exception:"}, "repository": "acme/service-01", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_31.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-01\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    token = str(random.random())\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_31.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-01\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_31.py\n+++ b/src/change_31.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    token = str(random.random())\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-330", "end_line": 52, "path": "src/change_31.py", "rule_id": "SEC-WEAK-RANDOM", "severity": "medium", "start_line": 52}], "id": "pr-0004", "pull_request": 1004, "repair_validation": {"auto_fixable": false, "required_after_patterns": ["secrets\\.token"], "risk_pattern": "random\\.random"}, "repository": "acme/service-01", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_01.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-01\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    token = \"test-placeholder\"\n    checksum = hashlib.md5(b\"fixture-id\").hexdigest()\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_01.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-01\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_01.py\n+++ b/src/clean_01.py\n@@ -21,33 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    token = \"test-placeholder\"\n+    checksum = hashlib.md5(b\"fixture-id\").hexdigest()\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0005", "pull_request": 1005, "repair_validation": {}, "repository": "acme/service-01", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_11.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-01\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    limit = min(max(int(value), 1), 100)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_11.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-01\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_11.py\n+++ b/src/clean_11.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    limit = min(max(int(value), 1), 100)\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0006", "pull_request": 1006, "repair_validation": {}, "repository": "acme/service-01", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_21.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-01\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    limit = min(max(int(value), 1), 100)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_21.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-01\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_21.py\n+++ b/src/clean_21.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    limit = min(max(int(value), 1), 100)\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0007", "pull_request": 1007, "repair_validation": {}, "repository": "acme/service-01", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_31.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-01\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    limit = min(max(int(value), 1), 100)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_31.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-01\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_31.py\n+++ b/src/clean_31.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    limit = min(max(int(value), 1), 100)\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0008", "pull_request": 1008, "repair_validation": {}, "repository": "acme/service-01", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_41.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-01\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    limit = min(max(int(value), 1), 100)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_41.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-01\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_41.py\n+++ b/src/clean_41.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    limit = min(max(int(value), 1), 100)\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0009", "pull_request": 1009, "repair_validation": {}, "repository": "acme/service-01", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_51.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-01\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    limit = min(max(int(value), 1), 100)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_51.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-01\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_51.py\n+++ b/src/clean_51.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    limit = min(max(int(value), 1), 100)\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0010", "pull_request": 1010, "repair_validation": {}, "repository": "acme/service-01", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_02.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-02\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    result = eval(value)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_02.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-02\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_02.py\n+++ b/src/change_02.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    result = eval(value)\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-95", "end_line": 52, "path": "src/change_02.py", "rule_id": "SEC-EVAL", "severity": "critical", "start_line": 52}], "id": "pr-0011", "pull_request": 1001, "repair_validation": {"auto_fixable": true, "required_after_patterns": ["json\\.loads"], "risk_pattern": "\\beval\\s*\\("}, "repository": "acme/service-02", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_12.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-02\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    api_key = \"production-secret-4\"\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_12.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-02\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_12.py\n+++ b/src/change_12.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    api_key = \"production-secret-4\"\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-798", "end_line": 52, "path": "src/change_12.py", "rule_id": "SEC-HARDCODED-SECRET", "severity": "high", "start_line": 52}], "id": "pr-0012", "pull_request": 1002, "repair_validation": {"auto_fixable": true, "required_after_patterns": ["os\\.environ"], "risk_pattern": "api_key\\s*=\\s*['\\\"]production-secret"}, "repository": "acme/service-02", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_22.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-02\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    try:\n        return int(value)\n    except Exception:\n        return None\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_22.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-02\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    try:\n        return int(value)\n    except ValueError:\n        return None\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_22.py\n+++ b/src/change_22.py\n@@ -21,36 +21,42 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     try:\n         return int(value)\n-    except ValueError:\n+    except Exception:\n         return None\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-703", "end_line": 53, "path": "src/change_22.py", "rule_id": "REL-EMPTY-EXCEPT", "severity": "medium", "start_line": 53}], "id": "pr-0013", "pull_request": 1003, "repair_validation": {"auto_fixable": true, "required_after_patterns": ["except ValueError:"], "risk_pattern": "except Exception:"}, "repository": "acme/service-02", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_32.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-02\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    while True:\n        if send(value):\n            return True\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_32.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-02\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    for attempt in range(3):\n        if send(value):\n            return True\n    return False\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_32.py\n+++ b/src/change_32.py\n@@ -21,36 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n-    for attempt in range(3):\n+    while True:\n         if send(value):\n             return True\n-    return False\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-835", "end_line": 51, "path": "src/change_32.py", "rule_id": "REL-UNBOUNDED-RETRY", "severity": "medium", "start_line": 51}], "id": "pr-0014", "pull_request": 1004, "repair_validation": {"auto_fixable": false, "required_after_patterns": ["max_attempts"], "risk_pattern": "while True:"}, "repository": "acme/service-02", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_02.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-02\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    token = \"test-placeholder\"\n    checksum = hashlib.md5(b\"fixture-id\").hexdigest()\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_02.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-02\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_02.py\n+++ b/src/clean_02.py\n@@ -21,33 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    token = \"test-placeholder\"\n+    checksum = hashlib.md5(b\"fixture-id\").hexdigest()\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0015", "pull_request": 1005, "repair_validation": {}, "repository": "acme/service-02", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_12.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-02\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    cursor.execute(\"SELECT * FROM users WHERE id = ?\", (value,))\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_12.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-02\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_12.py\n+++ b/src/clean_12.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    cursor.execute(\"SELECT * FROM users WHERE id = ?\", (value,))\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0016", "pull_request": 1006, "repair_validation": {}, "repository": "acme/service-02", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_22.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-02\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    cursor.execute(\"SELECT * FROM users WHERE id = ?\", (value,))\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_22.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-02\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_22.py\n+++ b/src/clean_22.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    cursor.execute(\"SELECT * FROM users WHERE id = ?\", (value,))\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0017", "pull_request": 1007, "repair_validation": {}, "repository": "acme/service-02", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_32.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-02\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    cursor.execute(\"SELECT * FROM users WHERE id = ?\", (value,))\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_32.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-02\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_32.py\n+++ b/src/clean_32.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    cursor.execute(\"SELECT * FROM users WHERE id = ?\", (value,))\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0018", "pull_request": 1008, "repair_validation": {}, "repository": "acme/service-02", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_42.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-02\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    cursor.execute(\"SELECT * FROM users WHERE id = ?\", (value,))\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_42.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-02\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_42.py\n+++ b/src/clean_42.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    cursor.execute(\"SELECT * FROM users WHERE id = ?\", (value,))\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0019", "pull_request": 1009, "repair_validation": {}, "repository": "acme/service-02", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_52.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-02\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    cursor.execute(\"SELECT * FROM users WHERE id = ?\", (value,))\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_52.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-02\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_52.py\n+++ b/src/clean_52.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    cursor.execute(\"SELECT * FROM users WHERE id = ?\", (value,))\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0020", "pull_request": 1010, "repair_validation": {}, "repository": "acme/service-02", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_03.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-03\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    result = eval(value)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_03.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-03\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_03.py\n+++ b/src/change_03.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    result = eval(value)\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-95", "end_line": 52, "path": "src/change_03.py", "rule_id": "SEC-EVAL", "severity": "critical", "start_line": 52}], "id": "pr-0021", "pull_request": 1001, "repair_validation": {"auto_fixable": true, "required_after_patterns": ["json\\.loads"], "risk_pattern": "\\beval\\s*\\("}, "repository": "acme/service-03", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_13.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-03\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    cursor.execute(\"SELECT * FROM users WHERE id=\" + value)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_13.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-03\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_13.py\n+++ b/src/change_13.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    cursor.execute(\"SELECT * FROM users WHERE id=\" + value)\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-89", "end_line": 52, "path": "src/change_13.py", "rule_id": "SEC-SQL-CONCAT", "severity": "high", "start_line": 52}], "id": "pr-0022", "pull_request": 1002, "repair_validation": {"auto_fixable": true, "required_after_patterns": ["\\(value,\\)"], "risk_pattern": "cursor\\.execute\\(.+\\+"}, "repository": "acme/service-03", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_23.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-03\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    try:\n        return int(value)\n    except Exception:\n        return None\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_23.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-03\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    try:\n        return int(value)\n    except ValueError:\n        return None\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_23.py\n+++ b/src/change_23.py\n@@ -21,36 +21,42 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     try:\n         return int(value)\n-    except ValueError:\n+    except Exception:\n         return None\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-703", "end_line": 53, "path": "src/change_23.py", "rule_id": "REL-EMPTY-EXCEPT", "severity": "medium", "start_line": 53}], "id": "pr-0023", "pull_request": 1003, "repair_validation": {"auto_fixable": true, "required_after_patterns": ["except ValueError:"], "risk_pattern": "except Exception:"}, "repository": "acme/service-03", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_33.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-03\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    assert user.is_admin\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_33.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-03\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_33.py\n+++ b/src/change_33.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    assert user.is_admin\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-617", "end_line": 52, "path": "src/change_33.py", "rule_id": "SEC-ASSERT-AUTH", "severity": "medium", "start_line": 52}], "id": "pr-0024", "pull_request": 1004, "repair_validation": {"auto_fixable": false, "required_after_patterns": ["PermissionError"], "risk_pattern": "assert user\\.is_admin"}, "repository": "acme/service-03", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_03.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-03\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    token = \"test-placeholder\"\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_03.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-03\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_03.py\n+++ b/src/clean_03.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    token = \"test-placeholder\"\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0025", "pull_request": 1005, "repair_validation": {}, "repository": "acme/service-03", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_13.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-03\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    api_key = os.environ[\"API_KEY\"]\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_13.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-03\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_13.py\n+++ b/src/clean_13.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    api_key = os.environ[\"API_KEY\"]\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0026", "pull_request": 1006, "repair_validation": {}, "repository": "acme/service-03", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_23.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-03\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    api_key = os.environ[\"API_KEY\"]\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_23.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-03\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_23.py\n+++ b/src/clean_23.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    api_key = os.environ[\"API_KEY\"]\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0027", "pull_request": 1007, "repair_validation": {}, "repository": "acme/service-03", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_33.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-03\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    api_key = os.environ[\"API_KEY\"]\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_33.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-03\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_33.py\n+++ b/src/clean_33.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    api_key = os.environ[\"API_KEY\"]\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0028", "pull_request": 1008, "repair_validation": {}, "repository": "acme/service-03", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_43.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-03\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    api_key = os.environ[\"API_KEY\"]\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_43.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-03\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_43.py\n+++ b/src/clean_43.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    api_key = os.environ[\"API_KEY\"]\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0029", "pull_request": 1009, "repair_validation": {}, "repository": "acme/service-03", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_53.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-03\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    api_key = os.environ[\"API_KEY\"]\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_53.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-03\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_53.py\n+++ b/src/clean_53.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    api_key = os.environ[\"API_KEY\"]\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0030", "pull_request": 1010, "repair_validation": {}, "repository": "acme/service-03", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_04.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-04\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    result = eval(value)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_04.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-04\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_04.py\n+++ b/src/change_04.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    result = eval(value)\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-95", "end_line": 52, "path": "src/change_04.py", "rule_id": "SEC-EVAL", "severity": "critical", "start_line": 52}], "id": "pr-0031", "pull_request": 1001, "repair_validation": {"auto_fixable": true, "required_after_patterns": ["json\\.loads"], "risk_pattern": "\\beval\\s*\\("}, "repository": "acme/service-04", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_14.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-04\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    cursor.execute(\"SELECT * FROM users WHERE id=\" + value)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_14.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-04\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_14.py\n+++ b/src/change_14.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    cursor.execute(\"SELECT * FROM users WHERE id=\" + value)\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-89", "end_line": 52, "path": "src/change_14.py", "rule_id": "SEC-SQL-CONCAT", "severity": "high", "start_line": 52}], "id": "pr-0032", "pull_request": 1002, "repair_validation": {"auto_fixable": true, "required_after_patterns": ["\\(value,\\)"], "risk_pattern": "cursor\\.execute\\(.+\\+"}, "repository": "acme/service-04", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_24.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-04\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    try:\n        return int(value)\n    except Exception:\n        return None\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_24.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-04\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    try:\n        return int(value)\n    except ValueError:\n        return None\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_24.py\n+++ b/src/change_24.py\n@@ -21,36 +21,42 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     try:\n         return int(value)\n-    except ValueError:\n+    except Exception:\n         return None\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-703", "end_line": 53, "path": "src/change_24.py", "rule_id": "REL-EMPTY-EXCEPT", "severity": "medium", "start_line": 53}], "id": "pr-0033", "pull_request": 1003, "repair_validation": {"auto_fixable": true, "required_after_patterns": ["except ValueError:"], "risk_pattern": "except Exception:"}, "repository": "acme/service-04", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_34.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-04\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    response.set_cookie(\"sid\", value, secure=False)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_34.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-04\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_34.py\n+++ b/src/change_34.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    response.set_cookie(\"sid\", value, secure=False)\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-614", "end_line": 52, "path": "src/change_34.py", "rule_id": "SEC-INSECURE-COOKIE", "severity": "medium", "start_line": 52}], "id": "pr-0034", "pull_request": 1004, "repair_validation": {"auto_fixable": false, "required_after_patterns": ["secure\\s*=\\s*True"], "risk_pattern": "secure\\s*=\\s*False"}, "repository": "acme/service-04", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_04.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-04\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    token = \"test-placeholder\"\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_04.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-04\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_04.py\n+++ b/src/clean_04.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    token = \"test-placeholder\"\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0035", "pull_request": 1005, "repair_validation": {}, "repository": "acme/service-04", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_14.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-04\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    digest = hashlib.sha256(str(value).encode()).hexdigest()\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_14.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-04\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_14.py\n+++ b/src/clean_14.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    digest = hashlib.sha256(str(value).encode()).hexdigest()\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0036", "pull_request": 1006, "repair_validation": {}, "repository": "acme/service-04", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_24.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-04\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    digest = hashlib.sha256(str(value).encode()).hexdigest()\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_24.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-04\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_24.py\n+++ b/src/clean_24.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    digest = hashlib.sha256(str(value).encode()).hexdigest()\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0037", "pull_request": 1007, "repair_validation": {}, "repository": "acme/service-04", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_34.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-04\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    digest = hashlib.sha256(str(value).encode()).hexdigest()\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_34.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-04\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_34.py\n+++ b/src/clean_34.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    digest = hashlib.sha256(str(value).encode()).hexdigest()\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0038", "pull_request": 1008, "repair_validation": {}, "repository": "acme/service-04", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_44.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-04\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    digest = hashlib.sha256(str(value).encode()).hexdigest()\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_44.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-04\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_44.py\n+++ b/src/clean_44.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    digest = hashlib.sha256(str(value).encode()).hexdigest()\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0039", "pull_request": 1009, "repair_validation": {}, "repository": "acme/service-04", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_54.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-04\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    digest = hashlib.sha256(str(value).encode()).hexdigest()\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_54.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-04\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_54.py\n+++ b/src/clean_54.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    digest = hashlib.sha256(str(value).encode()).hexdigest()\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0040", "pull_request": 1010, "repair_validation": {}, "repository": "acme/service-04", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_05.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-05\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    result = subprocess.run(value, shell=True)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_05.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-05\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_05.py\n+++ b/src/change_05.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    result = subprocess.run(value, shell=True)\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-78", "end_line": 52, "path": "src/change_05.py", "rule_id": "SEC-SUBPROCESS-SHELL", "severity": "high", "start_line": 52}], "id": "pr-0041", "pull_request": 1001, "repair_validation": {"auto_fixable": true, "required_after_patterns": ["shell\\s*=\\s*False"], "risk_pattern": "shell\\s*=\\s*True"}, "repository": "acme/service-05", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_15.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-05\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    cursor.execute(\"SELECT * FROM users WHERE id=\" + value)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_15.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-05\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_15.py\n+++ b/src/change_15.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    cursor.execute(\"SELECT * FROM users WHERE id=\" + value)\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-89", "end_line": 52, "path": "src/change_15.py", "rule_id": "SEC-SQL-CONCAT", "severity": "high", "start_line": 52}], "id": "pr-0042", "pull_request": 1002, "repair_validation": {"auto_fixable": true, "required_after_patterns": ["\\(value,\\)"], "risk_pattern": "cursor\\.execute\\(.+\\+"}, "repository": "acme/service-05", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_25.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-05\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    print(value)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_25.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-05\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_25.py\n+++ b/src/change_25.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    print(value)\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-532", "end_line": 52, "path": "src/change_25.py", "rule_id": "REL-DEBUG-PRINT", "severity": "low", "start_line": 52}], "id": "pr-0043", "pull_request": 1003, "repair_validation": {"auto_fixable": true, "required_after_patterns": ["return value"], "risk_pattern": "print\\s*\\("}, "repository": "acme/service-05", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_35.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-05\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    total = float(value) * 100\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_35.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-05\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_35.py\n+++ b/src/change_35.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    total = float(value) * 100\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-682", "end_line": 52, "path": "src/change_35.py", "rule_id": "REL-FLOAT-MONEY", "severity": "medium", "start_line": 52}], "id": "pr-0044", "pull_request": 1004, "repair_validation": {"auto_fixable": false, "required_after_patterns": ["Decimal"], "risk_pattern": "float\\(value\\)"}, "repository": "acme/service-05", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_05.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-05\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    token = \"test-placeholder\"\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_05.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-05\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_05.py\n+++ b/src/clean_05.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    token = \"test-placeholder\"\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0045", "pull_request": 1005, "repair_validation": {}, "repository": "acme/service-05", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_15.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-05\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    response.set_cookie(\"sid\", value, secure=True)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_15.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-05\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_15.py\n+++ b/src/clean_15.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    response.set_cookie(\"sid\", value, secure=True)\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0046", "pull_request": 1006, "repair_validation": {}, "repository": "acme/service-05", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_25.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-05\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    response.set_cookie(\"sid\", value, secure=True)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_25.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-05\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_25.py\n+++ b/src/clean_25.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    response.set_cookie(\"sid\", value, secure=True)\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0047", "pull_request": 1007, "repair_validation": {}, "repository": "acme/service-05", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_35.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-05\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    response.set_cookie(\"sid\", value, secure=True)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_35.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-05\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_35.py\n+++ b/src/clean_35.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    response.set_cookie(\"sid\", value, secure=True)\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0048", "pull_request": 1008, "repair_validation": {}, "repository": "acme/service-05", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_45.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-05\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    response.set_cookie(\"sid\", value, secure=True)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_45.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-05\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_45.py\n+++ b/src/clean_45.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    response.set_cookie(\"sid\", value, secure=True)\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0049", "pull_request": 1009, "repair_validation": {}, "repository": "acme/service-05", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_55.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-05\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    response.set_cookie(\"sid\", value, secure=True)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_55.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-05\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_55.py\n+++ b/src/clean_55.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    response.set_cookie(\"sid\", value, secure=True)\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0050", "pull_request": 1010, "repair_validation": {}, "repository": "acme/service-05", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_06.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-06\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    result = subprocess.run(value, shell=True)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_06.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-06\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_06.py\n+++ b/src/change_06.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    result = subprocess.run(value, shell=True)\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-78", "end_line": 52, "path": "src/change_06.py", "rule_id": "SEC-SUBPROCESS-SHELL", "severity": "high", "start_line": 52}], "id": "pr-0051", "pull_request": 1001, "repair_validation": {"auto_fixable": true, "required_after_patterns": ["shell\\s*=\\s*False"], "risk_pattern": "shell\\s*=\\s*True"}, "repository": "acme/service-06", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_16.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-06\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    cursor.execute(\"SELECT * FROM users WHERE id=\" + value)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_16.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-06\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_16.py\n+++ b/src/change_16.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    cursor.execute(\"SELECT * FROM users WHERE id=\" + value)\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-89", "end_line": 52, "path": "src/change_16.py", "rule_id": "SEC-SQL-CONCAT", "severity": "high", "start_line": 52}], "id": "pr-0052", "pull_request": 1002, "repair_validation": {"auto_fixable": true, "required_after_patterns": ["\\(value,\\)"], "risk_pattern": "cursor\\.execute\\(.+\\+"}, "repository": "acme/service-06", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_26.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-06\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    print(value)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_26.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-06\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_26.py\n+++ b/src/change_26.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    print(value)\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-532", "end_line": 52, "path": "src/change_26.py", "rule_id": "REL-DEBUG-PRINT", "severity": "low", "start_line": 52}], "id": "pr-0053", "pull_request": 1003, "repair_validation": {"auto_fixable": true, "required_after_patterns": ["return value"], "risk_pattern": "print\\s*\\("}, "repository": "acme/service-06", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_36.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-06\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    expires_at = datetime.now()\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_36.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-06\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_36.py\n+++ b/src/change_36.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    expires_at = datetime.now()\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-367", "end_line": 52, "path": "src/change_36.py", "rule_id": "REL-NAIVE-DATETIME", "severity": "medium", "start_line": 52}], "id": "pr-0054", "pull_request": 1004, "repair_validation": {"auto_fixable": false, "required_after_patterns": ["timezone\\.utc"], "risk_pattern": "datetime\\.now\\(\\)"}, "repository": "acme/service-06", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_06.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-06\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    limit = min(max(int(value), 1), 100)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_06.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-06\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_06.py\n+++ b/src/clean_06.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    limit = min(max(int(value), 1), 100)\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0055", "pull_request": 1005, "repair_validation": {}, "repository": "acme/service-06", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_16.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-06\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    limit = min(max(int(value), 1), 100)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_16.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-06\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_16.py\n+++ b/src/clean_16.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    limit = min(max(int(value), 1), 100)\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0056", "pull_request": 1006, "repair_validation": {}, "repository": "acme/service-06", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_26.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-06\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    limit = min(max(int(value), 1), 100)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_26.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-06\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_26.py\n+++ b/src/clean_26.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    limit = min(max(int(value), 1), 100)\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0057", "pull_request": 1007, "repair_validation": {}, "repository": "acme/service-06", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_36.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-06\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    limit = min(max(int(value), 1), 100)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_36.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-06\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_36.py\n+++ b/src/clean_36.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    limit = min(max(int(value), 1), 100)\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0058", "pull_request": 1008, "repair_validation": {}, "repository": "acme/service-06", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_46.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-06\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    limit = min(max(int(value), 1), 100)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_46.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-06\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_46.py\n+++ b/src/clean_46.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    limit = min(max(int(value), 1), 100)\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0059", "pull_request": 1009, "repair_validation": {}, "repository": "acme/service-06", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_56.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-06\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    limit = min(max(int(value), 1), 100)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_56.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-06\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_56.py\n+++ b/src/clean_56.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    limit = min(max(int(value), 1), 100)\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0060", "pull_request": 1010, "repair_validation": {}, "repository": "acme/service-06", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_07.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-07\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    result = subprocess.run(value, shell=True)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_07.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-07\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_07.py\n+++ b/src/change_07.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    result = subprocess.run(value, shell=True)\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-78", "end_line": 52, "path": "src/change_07.py", "rule_id": "SEC-SUBPROCESS-SHELL", "severity": "high", "start_line": 52}], "id": "pr-0061", "pull_request": 1001, "repair_validation": {"auto_fixable": true, "required_after_patterns": ["shell\\s*=\\s*False"], "risk_pattern": "shell\\s*=\\s*True"}, "repository": "acme/service-07", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_17.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-07\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return open(base / user_path).read()\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_17.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-07\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_17.py\n+++ b/src/change_17.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    return open(base / user_path).read()\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-22", "end_line": 52, "path": "src/change_17.py", "rule_id": "SEC-PATH-TRAVERSAL", "severity": "high", "start_line": 52}], "id": "pr-0062", "pull_request": 1002, "repair_validation": {"auto_fixable": true, "required_after_patterns": ["read_under_base"], "risk_pattern": "open\\(base\\s*/\\s*user_path\\)"}, "repository": "acme/service-07", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_27.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-07\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    print(value)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_27.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-07\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_27.py\n+++ b/src/change_27.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    print(value)\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-532", "end_line": 52, "path": "src/change_27.py", "rule_id": "REL-DEBUG-PRINT", "severity": "low", "start_line": 52}], "id": "pr-0063", "pull_request": 1003, "repair_validation": {"auto_fixable": true, "required_after_patterns": ["return value"], "risk_pattern": "print\\s*\\("}, "repository": "acme/service-07", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_37.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-07\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    time.sleep(5)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_37.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-07\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_37.py\n+++ b/src/change_37.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    time.sleep(5)\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-400", "end_line": 52, "path": "src/change_37.py", "rule_id": "REL-BLOCKING-ASYNC", "severity": "medium", "start_line": 52}], "id": "pr-0064", "pull_request": 1004, "repair_validation": {"auto_fixable": false, "required_after_patterns": ["await asyncio\\.sleep"], "risk_pattern": "time\\.sleep"}, "repository": "acme/service-07", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_07.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-07\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    cursor.execute(\"SELECT * FROM users WHERE id = ?\", (value,))\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_07.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-07\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_07.py\n+++ b/src/clean_07.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    cursor.execute(\"SELECT * FROM users WHERE id = ?\", (value,))\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0065", "pull_request": 1005, "repair_validation": {}, "repository": "acme/service-07", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_17.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-07\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    cursor.execute(\"SELECT * FROM users WHERE id = ?\", (value,))\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_17.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-07\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_17.py\n+++ b/src/clean_17.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    cursor.execute(\"SELECT * FROM users WHERE id = ?\", (value,))\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0066", "pull_request": 1006, "repair_validation": {}, "repository": "acme/service-07", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_27.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-07\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    cursor.execute(\"SELECT * FROM users WHERE id = ?\", (value,))\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_27.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-07\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_27.py\n+++ b/src/clean_27.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    cursor.execute(\"SELECT * FROM users WHERE id = ?\", (value,))\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0067", "pull_request": 1007, "repair_validation": {}, "repository": "acme/service-07", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_37.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-07\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    cursor.execute(\"SELECT * FROM users WHERE id = ?\", (value,))\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_37.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-07\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_37.py\n+++ b/src/clean_37.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    cursor.execute(\"SELECT * FROM users WHERE id = ?\", (value,))\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0068", "pull_request": 1008, "repair_validation": {}, "repository": "acme/service-07", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_47.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-07\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    cursor.execute(\"SELECT * FROM users WHERE id = ?\", (value,))\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_47.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-07\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_47.py\n+++ b/src/clean_47.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    cursor.execute(\"SELECT * FROM users WHERE id = ?\", (value,))\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0069", "pull_request": 1009, "repair_validation": {}, "repository": "acme/service-07", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_57.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-07\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    cursor.execute(\"SELECT * FROM users WHERE id = ?\", (value,))\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_57.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-07\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_57.py\n+++ b/src/clean_57.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    cursor.execute(\"SELECT * FROM users WHERE id = ?\", (value,))\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0070", "pull_request": 1010, "repair_validation": {}, "repository": "acme/service-07", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_08.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-08\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    result = subprocess.run(value, shell=True)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_08.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-08\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_08.py\n+++ b/src/change_08.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    result = subprocess.run(value, shell=True)\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-78", "end_line": 52, "path": "src/change_08.py", "rule_id": "SEC-SUBPROCESS-SHELL", "severity": "high", "start_line": 52}], "id": "pr-0071", "pull_request": 1001, "repair_validation": {"auto_fixable": true, "required_after_patterns": ["shell\\s*=\\s*False"], "risk_pattern": "shell\\s*=\\s*True"}, "repository": "acme/service-08", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_18.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-08\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return yaml.load(value)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_18.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-08\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_18.py\n+++ b/src/change_18.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    return yaml.load(value)\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-502", "end_line": 52, "path": "src/change_18.py", "rule_id": "SEC-YAML-LOAD", "severity": "high", "start_line": 52}], "id": "pr-0072", "pull_request": 1002, "repair_validation": {"auto_fixable": false, "required_after_patterns": ["yaml\\.safe_load"], "risk_pattern": "yaml\\.load"}, "repository": "acme/service-08", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_28.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-08\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    print(value)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_28.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-08\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_28.py\n+++ b/src/change_28.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    print(value)\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-532", "end_line": 52, "path": "src/change_28.py", "rule_id": "REL-DEBUG-PRINT", "severity": "low", "start_line": 52}], "id": "pr-0073", "pull_request": 1003, "repair_validation": {"auto_fixable": true, "required_after_patterns": ["return value"], "risk_pattern": "print\\s*\\("}, "repository": "acme/service-08", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_38.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-08\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    open(\"state.json\", \"w\").write(value)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_38.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-08\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_38.py\n+++ b/src/change_38.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    open(\"state.json\", \"w\").write(value)\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-362", "end_line": 52, "path": "src/change_38.py", "rule_id": "REL-NONATOMIC-WRITE", "severity": "medium", "start_line": 52}], "id": "pr-0074", "pull_request": 1004, "repair_validation": {"auto_fixable": false, "required_after_patterns": ["os\\.replace"], "risk_pattern": "open\\(.+state\\.json"}, "repository": "acme/service-08", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_08.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-08\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    api_key = os.environ[\"API_KEY\"]\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_08.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-08\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_08.py\n+++ b/src/clean_08.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    api_key = os.environ[\"API_KEY\"]\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0075", "pull_request": 1005, "repair_validation": {}, "repository": "acme/service-08", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_18.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-08\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    api_key = os.environ[\"API_KEY\"]\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_18.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-08\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_18.py\n+++ b/src/clean_18.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    api_key = os.environ[\"API_KEY\"]\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0076", "pull_request": 1006, "repair_validation": {}, "repository": "acme/service-08", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_28.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-08\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    api_key = os.environ[\"API_KEY\"]\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_28.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-08\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_28.py\n+++ b/src/clean_28.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    api_key = os.environ[\"API_KEY\"]\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0077", "pull_request": 1007, "repair_validation": {}, "repository": "acme/service-08", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_38.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-08\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    api_key = os.environ[\"API_KEY\"]\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_38.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-08\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_38.py\n+++ b/src/clean_38.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    api_key = os.environ[\"API_KEY\"]\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0078", "pull_request": 1008, "repair_validation": {}, "repository": "acme/service-08", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_48.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-08\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    api_key = os.environ[\"API_KEY\"]\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_48.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-08\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_48.py\n+++ b/src/clean_48.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    api_key = os.environ[\"API_KEY\"]\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0079", "pull_request": 1009, "repair_validation": {}, "repository": "acme/service-08", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/clean_58.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-08\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    api_key = os.environ[\"API_KEY\"]\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_58.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-08\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_58.py\n+++ b/src/clean_58.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    api_key = os.environ[\"API_KEY\"]\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0080", "pull_request": 1010, "repair_validation": {}, "repository": "acme/service-08", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "validation"}
{"after_files": {"src/change_09.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-09\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    api_key = \"production-secret-1\"\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_09.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-09\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_09.py\n+++ b/src/change_09.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    api_key = \"production-secret-1\"\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-798", "end_line": 52, "path": "src/change_09.py", "rule_id": "SEC-HARDCODED-SECRET", "severity": "high", "start_line": 52}], "id": "pr-0081", "pull_request": 1001, "repair_validation": {"auto_fixable": true, "required_after_patterns": ["os\\.environ"], "risk_pattern": "api_key\\s*=\\s*['\\\"]production-secret"}, "repository": "acme/service-09", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "holdout"}
{"after_files": {"src/change_19.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-09\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return pickle.loads(value)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_19.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-09\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_19.py\n+++ b/src/change_19.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    return pickle.loads(value)\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-502", "end_line": 52, "path": "src/change_19.py", "rule_id": "SEC-PICKLE-LOAD", "severity": "high", "start_line": 52}], "id": "pr-0082", "pull_request": 1002, "repair_validation": {"auto_fixable": false, "required_after_patterns": ["json\\.loads"], "risk_pattern": "pickle\\.loads"}, "repository": "acme/service-09", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "holdout"}
{"after_files": {"src/change_29.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-09\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    digest = hashlib.md5(value).hexdigest()\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_29.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-09\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_29.py\n+++ b/src/change_29.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    digest = hashlib.md5(value).hexdigest()\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-328", "end_line": 52, "path": "src/change_29.py", "rule_id": "SEC-WEAK-HASH", "severity": "medium", "start_line": 52}], "id": "pr-0083", "pull_request": 1003, "repair_validation": {"auto_fixable": false, "required_after_patterns": ["hashlib\\.sha256"], "risk_pattern": "hashlib\\.md5"}, "repository": "acme/service-09", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "holdout"}
{"after_files": {"src/change_39.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-09\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return redirect(value)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_39.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-09\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_39.py\n+++ b/src/change_39.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    return redirect(value)\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-601", "end_line": 52, "path": "src/change_39.py", "rule_id": "SEC-OPEN-REDIRECT", "severity": "medium", "start_line": 52}], "id": "pr-0084", "pull_request": 1004, "repair_validation": {"auto_fixable": false, "required_after_patterns": ["allowed_hosts"], "risk_pattern": "redirect\\(value\\)"}, "repository": "acme/service-09", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "holdout"}
{"after_files": {"src/clean_09.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-09\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    digest = hashlib.sha256(str(value).encode()).hexdigest()\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_09.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-09\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_09.py\n+++ b/src/clean_09.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    digest = hashlib.sha256(str(value).encode()).hexdigest()\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0085", "pull_request": 1005, "repair_validation": {}, "repository": "acme/service-09", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "holdout"}
{"after_files": {"src/clean_19.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-09\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    digest = hashlib.sha256(str(value).encode()).hexdigest()\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_19.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-09\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_19.py\n+++ b/src/clean_19.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    digest = hashlib.sha256(str(value).encode()).hexdigest()\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0086", "pull_request": 1006, "repair_validation": {}, "repository": "acme/service-09", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "holdout"}
{"after_files": {"src/clean_29.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-09\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    digest = hashlib.sha256(str(value).encode()).hexdigest()\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_29.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-09\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_29.py\n+++ b/src/clean_29.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    digest = hashlib.sha256(str(value).encode()).hexdigest()\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0087", "pull_request": 1007, "repair_validation": {}, "repository": "acme/service-09", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "holdout"}
{"after_files": {"src/clean_39.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-09\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    digest = hashlib.sha256(str(value).encode()).hexdigest()\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_39.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-09\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_39.py\n+++ b/src/clean_39.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    digest = hashlib.sha256(str(value).encode()).hexdigest()\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0088", "pull_request": 1008, "repair_validation": {}, "repository": "acme/service-09", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "holdout"}
{"after_files": {"src/clean_49.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-09\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    digest = hashlib.sha256(str(value).encode()).hexdigest()\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_49.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-09\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_49.py\n+++ b/src/clean_49.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    digest = hashlib.sha256(str(value).encode()).hexdigest()\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0089", "pull_request": 1009, "repair_validation": {}, "repository": "acme/service-09", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "holdout"}
{"after_files": {"src/clean_59.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-09\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    digest = hashlib.sha256(str(value).encode()).hexdigest()\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_59.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-09\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_59.py\n+++ b/src/clean_59.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    digest = hashlib.sha256(str(value).encode()).hexdigest()\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0090", "pull_request": 1010, "repair_validation": {}, "repository": "acme/service-09", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "holdout"}
{"after_files": {"src/change_10.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-10\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    api_key = \"production-secret-2\"\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_10.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-10\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_10.py\n+++ b/src/change_10.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    api_key = \"production-secret-2\"\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-798", "end_line": 52, "path": "src/change_10.py", "rule_id": "SEC-HARDCODED-SECRET", "severity": "high", "start_line": 52}], "id": "pr-0091", "pull_request": 1001, "repair_validation": {"auto_fixable": true, "required_after_patterns": ["os\\.environ"], "risk_pattern": "api_key\\s*=\\s*['\\\"]production-secret"}, "repository": "acme/service-10", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "holdout"}
{"after_files": {"src/change_20.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-10\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    try:\n        return int(value)\n    except Exception:\n        return None\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_20.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-10\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    try:\n        return int(value)\n    except ValueError:\n        return None\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_20.py\n+++ b/src/change_20.py\n@@ -21,36 +21,42 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     try:\n         return int(value)\n-    except ValueError:\n+    except Exception:\n         return None\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-703", "end_line": 53, "path": "src/change_20.py", "rule_id": "REL-EMPTY-EXCEPT", "severity": "medium", "start_line": 53}], "id": "pr-0092", "pull_request": 1002, "repair_validation": {"auto_fixable": true, "required_after_patterns": ["except ValueError:"], "risk_pattern": "except Exception:"}, "repository": "acme/service-10", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "holdout"}
{"after_files": {"src/change_30.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-10\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    path = tempfile.mktemp()\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_30.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-10\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_30.py\n+++ b/src/change_30.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    path = tempfile.mktemp()\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-377", "end_line": 52, "path": "src/change_30.py", "rule_id": "SEC-INSECURE-TEMPFILE", "severity": "medium", "start_line": 52}], "id": "pr-0093", "pull_request": 1003, "repair_validation": {"auto_fixable": false, "required_after_patterns": ["NamedTemporaryFile"], "risk_pattern": "tempfile\\.mktemp"}, "repository": "acme/service-10", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "holdout"}
{"after_files": {"src/change_40.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-10\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    logger.info(value)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/change_40.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-10\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    normalized = str(value)\n    return normalized\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/change_40.py\n+++ b/src/change_40.py\n@@ -21,34 +21,41 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n     normalized = str(value)\n-    return normalized\n+    logger.info(value)\n+    return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [{"cwe": "CWE-117", "end_line": 52, "path": "src/change_40.py", "rule_id": "SEC-LOG-FORGING", "severity": "low", "start_line": 52}], "id": "pr-0094", "pull_request": 1004, "repair_validation": {"auto_fixable": false, "required_after_patterns": ["sanitize"], "risk_pattern": "logger\\.info\\(value\\)"}, "repository": "acme/service-10", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "holdout"}
{"after_files": {"src/clean_10.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-10\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    response.set_cookie(\"sid\", value, secure=True)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_10.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-10\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_10.py\n+++ b/src/clean_10.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    response.set_cookie(\"sid\", value, secure=True)\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0095", "pull_request": 1005, "repair_validation": {}, "repository": "acme/service-10", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "holdout"}
{"after_files": {"src/clean_20.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-10\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    response.set_cookie(\"sid\", value, secure=True)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_20.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-10\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_20.py\n+++ b/src/clean_20.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    response.set_cookie(\"sid\", value, secure=True)\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0096", "pull_request": 1006, "repair_validation": {}, "repository": "acme/service-10", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "holdout"}
{"after_files": {"src/clean_30.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-10\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    response.set_cookie(\"sid\", value, secure=True)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_30.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-10\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_30.py\n+++ b/src/clean_30.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    response.set_cookie(\"sid\", value, secure=True)\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0097", "pull_request": 1007, "repair_validation": {}, "repository": "acme/service-10", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "holdout"}
{"after_files": {"src/clean_40.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-10\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    response.set_cookie(\"sid\", value, secure=True)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_40.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-10\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_40.py\n+++ b/src/clean_40.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    response.set_cookie(\"sid\", value, secure=True)\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0098", "pull_request": 1008, "repair_validation": {}, "repository": "acme/service-10", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "holdout"}
{"after_files": {"src/clean_50.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-10\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    response.set_cookie(\"sid\", value, secure=True)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_50.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-10\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_50.py\n+++ b/src/clean_50.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    response.set_cookie(\"sid\", value, secure=True)\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0099", "pull_request": 1009, "repair_validation": {}, "repository": "acme/service-10", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "holdout"}
{"after_files": {"src/clean_60.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-10\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 2\nMAX_INPUT_SIZE = 8192\n\n\ndef build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n    normalized = normalize_request(value)\n    if len(normalized) > MAX_INPUT_SIZE:\n        raise ValueError(\"request value is too large\")\n    supplied = dict(attributes or {})\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n        \"attribute_count\": len(supplied),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    response.set_cookie(\"sid\", value, secure=True)\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n    return context\n"}, "before_files": {"src/clean_60.py": "\"\"\"Synthetic PR fixture; all names and values are fictional.\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nimport hashlib\nimport json\nimport os\nfrom pathlib import Path\nimport pickle\nimport random\nimport subprocess\nimport tempfile\nimport time\nfrom typing import Any, Mapping\n\nSERVICE_NAME = \"fixture-service-10\"\nDEFAULT_TIMEOUT_SECONDS = 5\n\n\ndef normalize_request(value: Any) -> str:\n    \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n    normalized = str(value).strip()\n    if not normalized:\n        raise ValueError(\"request value is required\")\n    return normalized\n\n\nSCHEMA_VERSION = 1\n\n\ndef build_request_context(value: Any) -> dict:\n    \"\"\"Build the original minimal request metadata.\"\"\"\n    normalized = normalize_request(value)\n    return {\n        \"service\": SERVICE_NAME,\n        \"schema_version\": SCHEMA_VERSION,\n        \"value_length\": len(normalized),\n    }\n\n\n# fixture-core:start\ndef process(value):\n    return value\n# fixture-core:end\n\ndef summarize_result(value: Any) -> dict:\n    \"\"\"Return metadata for an offline fixture result.\"\"\"\n    context = build_request_context(value)\n    context[\"status\"] = \"processed\"\n    return context\n"}, "diff": "--- a/src/clean_60.py\n+++ b/src/clean_60.py\n@@ -21,33 +21,40 @@\n \n \n def normalize_request(value: Any) -> str:\n     \"\"\"Normalize one synthetic request without retaining its original value.\"\"\"\n     normalized = str(value).strip()\n     if not normalized:\n         raise ValueError(\"request value is required\")\n     return normalized\n \n \n-SCHEMA_VERSION = 1\n+SCHEMA_VERSION = 2\n+MAX_INPUT_SIZE = 8192\n \n \n-def build_request_context(value: Any) -> dict:\n-    \"\"\"Build the original minimal request metadata.\"\"\"\n+def build_request_context(value: Any, attributes: Mapping[str, Any] | None = None) -> dict:\n+    \"\"\"Build bounded metadata used by the synthetic service pipeline.\"\"\"\n     normalized = normalize_request(value)\n+    if len(normalized) > MAX_INPUT_SIZE:\n+        raise ValueError(\"request value is too large\")\n+    supplied = dict(attributes or {})\n     return {\n         \"service\": SERVICE_NAME,\n         \"schema_version\": SCHEMA_VERSION,\n         \"value_length\": len(normalized),\n+        \"attribute_count\": len(supplied),\n     }\n \n \n # fixture-core:start\n def process(value):\n+    response.set_cookie(\"sid\", value, secure=True)\n     return value\n # fixture-core:end\n \n def summarize_result(value: Any) -> dict:\n-    \"\"\"Return metadata for an offline fixture result.\"\"\"\n+    \"\"\"Return stable metadata for an offline fixture result.\"\"\"\n     context = build_request_context(value)\n     context[\"status\"] = \"processed\"\n+    context[\"timeout_seconds\"] = DEFAULT_TIMEOUT_SECONDS\n     return context\n", "expected_findings": [], "id": "pr-0100", "pull_request": 1010, "repair_validation": {}, "repository": "acme/service-10", "schema_version": 1, "source": {"generator": "evoagent-e2e-v2-contextual", "kind": "offline-fixture", "public_url": null}, "split": "holdout"}
